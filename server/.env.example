# Copy to .env and set values. Never commit .env or real secrets.
# Use 4000 if 3000 is in use (e.g. by Vite frontend). Seed script uses this same PORT.
PORT=4000
NODE_ENV=development

# Required in production (app refuses to start if missing)
JWT_SECRET=your-super-secret-key-change-in-production

# CORS: comma-separated origins that may call the API (e.g. main app, dashboard). Empty = same-origin only.
# CORS_ALLOWED_ORIGINS=https://app.example.com,https://admin.example.com
# Dashboard app origin (optional): add to CORS so dashboard can call /api/dashboard/stats and /api/dashboard/users.
# DASHBOARD_ORIGIN=https://your-dashboard.vercel.app

# Supabase (optional; when using Supabase for DB/Auth)
# SUPABASE_URL=https://xxxxxxxx.supabase.co
# SUPABASE_SERVICE_ROLE_KEY=eyJ...
# SUPABASE_DB_URL=postgresql://postgres.[ref]:[PASSWORD]@aws-0-[region].pooler.supabase.com:5432/postgres
#   ^ For "reset-db-remote" only. From Dashboard → Settings → Database → Connection string (Session mode).

# Dashboard API: secret for /api/dashboard/stats and /api/dashboard/users. Set the same value as VITE_DASHBOARD_SECRET in the dashboard app.
# DASHBOARD_SECRET=your-dashboard-secret
# Optional: URL of the main app (shown in dashboard stats).
# APP_URL=https://myapp.com
# Optional: salt for hashing IP in visitor_sessions (never store raw IP).
# VISIT_HASH_SALT=random-string
# Optional: TTL in minutes for "visitors now" / "active users now" (default 5).
# NOW_TTL_MINUTES=5
# Optional: minutes with no activity after which a visit is not "active" (default 10). activeVisits = sessions with last_seen_at within this window.
# ACTIVE_VISIT_TTL_MINUTES=10
